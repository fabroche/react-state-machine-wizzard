import React from 'react'
import ReactDOM from 'react-dom/client'
import StateMachineWizzard from './StateMachineWizzard.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <StateMachineWizzard />
  </React.StrictMode>,
)
